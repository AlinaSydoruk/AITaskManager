{% extends 'profile.html.twig' %}

{% block title %}{{ 'profile.welcome' | trans  }}{% endblock %}


 {% block body %}{% endblock %}


{% block content %}
    <div>
        <twig:MainHeader text="{{ 'profile.welcome' | trans  }}"></twig:MainHeader>

        <div class="mb-3">
            {{ app.user.userIdentifier }}
        </div>

    </div>
    <a href="{{ path('app_employee_index') }}">
        <div class="my-4 sm:my-6 border-y-2   border-neutral">
            <div class="my-2 ml-16 md:ml-24 lg:ml-32  border-l-1   border-neutral"  >
            {{ 'profile.employees' | trans }}
            </div>
        </div>
    </a>

{% endblock content %}
