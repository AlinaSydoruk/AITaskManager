framework:
    default_locale: de_CH
    enabled_locales: ['en_US', 'de_CH']
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
        providers:
